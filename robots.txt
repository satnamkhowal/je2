User-agent: *
Allow: /

# Keep the private admin area out of crawler access.
Disallow: /admin/

# Installer and lead-processing endpoints intentionally remain crawlable so
# search engines can see their explicit noindex directives instead of leaving
# blocked URL-only entries in the index.

Sitemap: https://jaipurengineers.com/sitemap.xml
